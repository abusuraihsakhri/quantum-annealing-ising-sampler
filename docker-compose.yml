version: '3.8'

services:
  quantum-annealing-ising-sampler:
    build: .
    container_name: quantum-annealing-ising-sampler
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=${AUDIT_SECRET_KEY:-CHANGE_ME_GENERATE_A_RANDOM_KEY}
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
